apiVersion: v1
kind: Service
metadata:
  name: {{ include "symbi.fullname" . }}
  labels:
    {{- include "symbi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "symbi.selectorLabels" . | nindent 4 }}
